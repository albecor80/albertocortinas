export type pageData = {
    url: string;      // URL of the page (used for navigation and transitions)
    title: string;    // Title of the page
    date: string;     // Date the page was created or last updated
    author: string;   // Author's name
    summary: string;  // Summary or description of the page
};
